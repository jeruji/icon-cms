import speaker from "./speaker"
import speaker_type from "./speaker_type"
import schedule from "./schedule"
import about from "./about"

export const schemaTypes = [speaker, speaker_type, schedule, about]
